---
apiVersion: v1
kind: Service
metadata:
  name: space-app-v2-service
spec:
  type: LoadBalancer
  selector:
    app: space-app-v2
  ports:
  - port: 80
    targetPort: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: space-app-v2-deployment
  labels:
    app: space-app-v2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: space-app-v2
  template:
    metadata:
      labels:
        app: space-app-v2
    spec:
      containers:
      - name: space-app-v2
        image: us-central1-docker.pkg.dev/GOOGLE_PROJECT/space-app-v2/space-app-v2:latest